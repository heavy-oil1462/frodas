# Copy to .env — or better, run `python3 ../tools/stack.py init` which
# generates this with random passwords AND creates mosquitto/passwd to match.
MQTT_USER=frodas
MQTT_PASSWORD=change-me
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me
